import {randomState,sign} from '../../../lib/crypto.js';import {googleAuthUrl} from '../../../lib/google.js';import {requireSession} from '../../../lib/auth.js';import {fail} from '../../../lib/http.js';export default async function handler(req,res){try{requireSession(req);const state=randomState();res.setHeader('Set-Cookie',`google_state=${state}.${sign(state)}; Path=/; HttpOnly; Secure; SameSite=Lax; Max-Age=600`);res.redirect(302,googleAuthUrl(state));}catch(e){fail(res,e);}}
