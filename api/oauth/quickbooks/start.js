import {randomState,sign} from '../../../lib/crypto.js';import {qboAuthUrl} from '../../../lib/qbo.js';import {requireSession} from '../../../lib/auth.js';import {fail} from '../../../lib/http.js';export default async function handler(req,res){try{requireSession(req);const state=randomState();res.setHeader('Set-Cookie',`qbo_state=${state}.${sign(state)}; Path=/; HttpOnly; Secure; SameSite=Lax; Max-Age=600`);res.redirect(302,qboAuthUrl(state));}catch(e){fail(res,e);}}
