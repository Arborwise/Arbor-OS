import {json,fail,method} from '../lib/http.js';import {requireCronOrSession} from '../lib/auth.js';import {runSync} from '../lib/sync.js';export default async function handler(req,res){try{method(req,['GET','POST']);requireCronOrSession(req);const summary=await runSync(req.query?.scheduled?`scheduled:${req.query.scheduled}`:'manual');json(res,200,{ok:true,summary,syncedAt:new Date().toISOString()});}catch(e){fail(res,e);}}
