export function classify(text=''){const value=String(text||'').toLowerCase();if(/mow|edg(e|ing)|weed|lawn|landscap|mulch|irrigat|shrub/.test(value))return 'KW';if(/sbbmanagement|sbb management|\bsbb\b|sicily laguna|venetian hoa/.test(value))return 'SBB';if(/goodwin/.test(value))return 'GOODWIN';if(/kanam/.test(value))return 'KANAM';return 'RESIDENTIAL';}
export function kindFromStatus(status='',fallback='est'){return /approved|accepted|scheduled|in progress|converted|completed|done|invoiced|paid/i.test(status)?'job':fallback;}
export function isClosed(status=''){return /declined|rejected|closed|converted|completed|done|paid|cancelled|canceled/i.test(status);}
